server {
#    listen         643;       # Listen on port 8200 for IPv4 requests
#    listen [::]:643 default_server ipv6only=on;

    server_name     printer;

    access_log      /var/log/nginx/printer.access.log;
    error_log       /var/log/nginx/printer.error.log;

#    rewrite ^/mycloud/(.*)$ /$1;

    location ^~ (.*) {
        include /etc/nginx/proxy_params;
    }

}

